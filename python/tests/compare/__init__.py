"""Compare module for MATLAB vs Python CSV comparisons.

The small ``test_compare_*`` modules intentionally act as runner wrappers:
``run_comparison_suite`` performs the assertions and raises on comparison
failures.
"""
